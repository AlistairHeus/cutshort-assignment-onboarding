import React from "react";

const Success = ({ step }) => {
  return (
    <div>
      <h1>Succusessu</h1>
      <div>{step}</div>
      <button>Launch Eden</button>
    </div>
  );
};

export default Success;
